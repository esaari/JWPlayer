/**
 * Navigation tool bar that is present upon login and through all subsequent
 * player actions.
 * @type {{playerListLink: string}}
 */

const selectors = {
    "playerListLink": ".navigation-bar .publish [href=\"/#/players/list\"]"
};

export { selectors }